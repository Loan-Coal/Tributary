# Legacy requirements file — use pyproject.toml for the canonical dependency list.
# torch and transformers are NOT required; they were removed (see ISSUE-026).
# qwen_client.py guards those imports with try/except.
pydantic>=2.0,<3.0
PyYAML>=6.0
pytest>=8.0
anthropic>=0.30.0
httpx>=0.27
